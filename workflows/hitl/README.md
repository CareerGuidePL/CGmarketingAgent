# `hitl/` — zarezerwowane

**`cg-hitl-discord-reply`** (polling SeaTable → Discord) został **usunięty z repozytorium** (2026-04): HITL jest w **`cg-orchestrator-main`** (Discord `sendAndWait`).

Katalog zostaje na przyszłe workflowy `cg-hitl-*`, jeśli znowu będą potrzebne osobne procesy HITL.
